export interface MetaData {
  ubigeos: string[];
  totalPedidos: number;
  ubigeoCounts: Record<string, number>;
  estados: string[];
  supervisores: string[];
  choferes: string[];
  placas: string[];
}
